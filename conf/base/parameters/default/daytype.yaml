
prepare_for_daytyping:
  nan_threshold: 0.5 

find_prototypes:
  max_iter: 30
  early_stopping_val: 0.1 

learn_distance_metric:
  pairs_per_prototype: 50
  test_size: 0.25

apply_daytyping:
  window: 8
  n_jobs: 1 # make sure you have enough memory if choose to change it