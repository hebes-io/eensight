
prepare_data:
  nan_threshold: 0.5 

find_prototypes:
  max_iter: 30
  early_stopping_val: 0.1 

learn_distance_metric:
  pairs_per_prototype: 50
  test_size: 0.2

cluster_to_day_type:
  min_cluster_size: 30

daytype:
  window: 8
  n_jobs: 1