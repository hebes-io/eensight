site_name : ???
root_input_name: ??
versioned: ??? 

location:
  country   : null  
  province  : null 
  state     : null 

rebind_names:
  consumption : null
  temperature : null 
  holiday     : null 
  timestamp   : null


train.root_input:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.raw}/train/${root_input_name}.csv

test.root_input:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.raw}/test/${root_input_name}.csv

post.root_input:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.raw}/post/${root_input_name}.csv



