
train.data_with_drift:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.intermediate}/train/data_with_drift.csv
  load_args:
    sep: ','
    index_col : 0
    parse_dates : 
      - 0
  save_args:
    index: true
  versioned : ${versioned}

train.preprocessed_data:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.primary}/train/preprocessed.csv
  load_args:
    sep: ','
    index_col : 0
    parse_dates : 
      - 0
  save_args:
    index: true
  versioned : ${versioned}

apply.preprocessed_data:
  type: ${globals:types.csv}
  filepath: ${site_name}/${globals:folders.primary}/apply/preprocessed.csv
  load_args:
    sep: ','
    index_col : 0
    parse_dates :
      - 0
  save_args:
    index: true
  versioned : ${versioned}

train.model_input_data:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.model_input}/train/model_input.csv
  load_args:
    sep: ','
    index_col : 0
    parse_dates : 
      - 0
  save_args:
    index: true
  versioned : ${versioned}

apply.model_input_data:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.model_input}/apply/model_input.csv
  load_args:
    sep: ','
    index_col : 0
    parse_dates : 
      - 0
  save_args:
    index: true
  versioned : ${versioned}

train.data_adequacy:
  type: ${globals:types.csv}
  filepath: >-
    ${site_name}/${globals:folders.report}/train/adequacy_summary.csv
  load_args:
    sep: ','
    index_col : 0
  save_args:
    index: true
  versioned : ${versioned}

train.matrix_profile_scores:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.primary}/train/matrix_profile_scores.csv
  load_args:
    sep: ','
    index_col : 0
    parse_dates : 
      - 0
  save_args:
    index: true
  versioned : ${versioned}

train.prototype_days:
  type: ${globals:types.pickle}
  filepath: ${site_name}/${globals:folders.primary}/train/prototype_days.pickle
  versioned : ${versioned}

train.distance_metrics:
  type: ${globals:types.pickle}
  filepath: ${site_name}/${globals:folders.model_input}/train/distance_metrics.pickle
  versioned : ${versioned}

opt_params:
  type: ${globals:types.json}
  filepath: ${site_name}/${globals:folders.model}/train/opt_params.json
  versioned : ${versioned}

mean_model:
  type: ${globals:types.pickle}
  filepath: ${site_name}/${globals:folders.model}/train/mean_model.pickle
  versioned : ${versioned}

cv_model:
  type: ${globals:types.pickle}
  filepath: ${site_name}/${globals:folders.model}/train/cv_model.pickle
  versioned : ${versioned}

conformal_model:
  type: ${globals:types.pickle}
  filepath: ${site_name}/${globals:folders.model}/train/conformal_model.pickle
  versioned : ${versioned}

train.model_prediction:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.model_output}/train/model_prediction.csv
  load_args:
    sep: ','
    index_col : 0
    parse_dates : 
      - 0
  save_args:
    index: true
  versioned : ${versioned}
  
apply.model_prediction:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.model_output}/apply/model_prediction.csv
  load_args:
    sep: ','
    index_col : 0
    parse_dates : 
      - 0
  save_args:
    index: true
  versioned : ${versioned}

apply.savings:
  type: ${globals:types.csv}
  filepath: >- 
    ${site_name}/${globals:folders.model_output}/apply/savings.csv
  load_args:
    sep: ','
    index_col : 0
    parse_dates : 
      - 0
  save_args:
    index: true
  versioned : ${versioned}


