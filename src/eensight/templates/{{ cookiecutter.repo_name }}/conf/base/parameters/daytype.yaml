# parameters for the `daytype` pipeline

of_prepare_data:
  nan_threshold: 0.33

of_find_prototypes:
  max_iter: 30
  early_stopping_val: 0.1 

of_distance_learning:
  pairs_per_prototype: 0.15 # percentage of all profiles in the dataset
  test_size: 0.25

daytyping_window: 8 # always in hours
